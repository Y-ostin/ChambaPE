export enum RoleEnum {
  'admin' = 1,
  'user' = 2, // Usuario que contrata servicios
  'worker' = 3, // Trabajador que ofrece servicios
  'super_admin' = 4, // Super administrador de la plataforma
}
